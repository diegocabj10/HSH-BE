
const canDoThat = async (userId, endpointId) => {
    const profilesList = getProfilesByEndpoint(endpointId);
};

const getProfilesByEndpoint = async (userId, endpointId) => {


};


const permissionsByEndpoint = [




];

module.exports = canDoThat;